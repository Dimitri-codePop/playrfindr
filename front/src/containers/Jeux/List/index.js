import { connect } from 'react-redux';
import List from 'src/components/Jeux/List';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(List);
