import React, { useState } from 'react';
import PropTypes from 'prop-types';

import './style.scss';

export default function Login() {
  const [logginIsHidden, setLogginIsHidden] = useState(false);
  return (

);
}

Login.propTypes = {

};
